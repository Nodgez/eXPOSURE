name: Check for license in secrets 🔐

env:
    UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}

on: workflow_dispatch

jobs:
    checkLicense:
    name: Check for Unity License
    runs-on: ubuntu-latest
    steps:
      - name: Fail - No License
        if: ${{ !startsWith(env.UNITY_LICENSE,  '<') }}
        run: exit 1